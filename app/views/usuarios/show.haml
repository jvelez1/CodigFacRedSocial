.row.center-xs
  .col-xs-10.col-sm-8.col-md-6.col-lg-4
    .box
      .white.mdl-shadow--4dp.top-space.relative
        / cover
        %section.border-bottom.top-space
          .row.middle-xs
            .col-xs
              .box
                %h1.no-margin.medium
                  %span= best_in_place_if current_user == @user, @user, :username
                  %p
                    %span= best_in_place_if current_user == @user, @user, :name
                    %span= best_in_place_if current_user == @user, @user, :last_name
                    %span= best_in_place_if current_user == @user, @user, :email
            .col-xs-4.col-sm-4
              .box
              / boton follow

        .large-padding
          .row
            .col-xs
              .box
                Seguidores
            .col-xs
              .box
                =@user.posts.count
                Posts
          .row.center-xs
            .col-xs-8.col-md-10
              .box
                %span= best_in_place_if current_user == @user, @user, :bio
      .top-space